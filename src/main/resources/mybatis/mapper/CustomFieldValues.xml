<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mybatis.mapper.StationMapper">

    <resultMap id="customFieldValue" type="com.sevgmo.stationpassport.model.CustomFieldValue">

    </resultMap>
    <select id="selectStationCustomFieldValues" parameterType="java.lang.Integer">
        SELECT * FROM custom_field_values WHERE station_id=#value#
    </select>

</mapper>